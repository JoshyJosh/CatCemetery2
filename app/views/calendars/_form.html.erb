<%= form_for(@calendar) do |f| %>
  <% if @calendar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@calendar.errors.count, "error") %> prohibited this calendar from being saved:</h2>

      <ul>
      <% @calendar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<div class="field">
		<%= f.label :res_date %><br>
		<%= f.date_select(:res_date) %>
	</div>
	
  <div class="field">
    <%= f.label :res_date %><br>
    <%= f.time_select(:res_date, :discard_minute => true, prompt: {hour: Time.now.hour, minute: "00"})%>:00
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
